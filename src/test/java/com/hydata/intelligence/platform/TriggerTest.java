package com.hydata.intelligence.platform;

/**
 * @author: Jasmine
 * @createTime: 2018年11月20日上午11:35:16
 * @description: <触发器触发模块-测试主函数>
 * @modified:
 */
public class TriggerTest {
    public static void main(String[] args) throws InterruptedException {
//        BlockingQueue<DeviceController.LiveDataStream> queue = new LinkedBlockingDeque<>(10);
        /*
        TriggerProducer p1 = new TriggerProducer(queue);
        TriggerProducer p2 = new TriggerProducer(queue);
        TriggerProducer p3 = new TriggerProducer(queue);
        TriggerConsumer c1 = new TriggerConsumer(queue);
        TriggerConsumer c2 = new TriggerConsumer(queue);
        TriggerConsumer c3 = new TriggerConsumer(queue);
        ExecutorService service = Executors.newCachedThreadPool();
        service.execute(p1);
        service.execute(p2);
        service.execute(p3);
        service.execute(c1);
        service.execute(c2);
        service.execute(c3);
        Thread.sleep(10*1000);
        p1.stop();
        p2.stop();
        p3.stop();
        Thread.sleep(3000);
        service.shutdown();
        */
    }
}